name: Actions 😎

on:
  pull_request: {}
  push: { branches: [master] }

env:
  UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}

jobs:
  buildAndTestForSomePlatforms:
    name: Build for ${{ matrix.targetPlatform }} on version ${{ matrix.unityVersion }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        unityVersion:
          - 2019.2.2f1
        targetPlatform:
          - Android
#          - iOS
#          - WebGL
    steps:
      - uses: actions/checkout@v2
        with:
          lfs: true
      - name: update
        run: sudo apt-get update && sudo apt-get install -y locales && sudo rm -rf /var/lib/apt/lists/* && sudo localedef -i en_US -c -f UTF-8 -A /usr/share/locale/locale.alias en_US.UTF-8
#      - name: set up JDK 1.8
#        uses: actions/setup-java@v1
#        with:
#          java-version: 1.8
#      - name: install NDK
#        run: echo "y" | sudo ${ANDROID_HOME}/tools/bin/sdkmanager --install "ndk;20.0.5594570"
#      - uses: actions/cache@v1.1.0
#        with:
#          path: Library
#          key: Library
#      - uses: webbertakken/unity-test-runner@v1.3
#        id: testRunner
#        with:
#          unityVersion: ${{ matrix.unityVersion }}
#      - uses: actions/upload-artifact@v1
#        with:
#          name: Test results (all modes)
#          path: ${{ steps.testRunner.outputs.artifactsPath }}
      - uses: webbertakken/unity-builder@v0.10
        with:
          unityVersion: ${{ matrix.unityVersion }}
          targetPlatform: ${{ matrix.targetPlatform }}
#      - uses: actions/upload-artifact@v1
#        with:
#          name: Build
#          path: build
